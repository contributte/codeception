actor: Tester

paths:
    tests: tests
    log: tests/_log
    data: tests/_data
    support: tests/_helpers

bootstrap: _bootstrap.php

settings:
    log: true

coverage:
    enabled: true
    include:
        - src/*
