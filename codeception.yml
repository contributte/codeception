actor: Tester

paths:
    tests: tests
    log: tests/_log
    data: tests/_data
    support: tests/_helpers

settings:
    bootstrap: _bootstrap.php
    log: true

extensions:
    enabled:
        - Arachne\Codeception\Tracy\Logger

coverage:
    enabled: true
    include:
        - src/*
