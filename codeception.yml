actor: Tester

paths:
    tests: tests
    log: tests/_log
    data: tests/_data
    support: tests/_helpers

settings:
    bootstrap: _bootstrap.php
    log: true

coverage:
    enabled: true
    include:
        - src/*
